[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pythoncli_Ayamamoru"
version = "0.0.2"
dependencies = []

[project.scripts]
fishk = "pythoncli_Ayamamoru.main:main"
